# Локальная разработка: пустой базовый URL → axios бьёт в тот же origin (Vite), см. proxy в vite.config.ts.
VITE_API_BASE_URL=

# Куда Vite проксирует /api и /uploads (тот же бэкенд, что в .env для прод-сборки).
VITE_DEV_API_PROXY_TARGET=http://85.192.33.223:5000
