# Shell-скрипты запускаются на Linux. С CRLF bash читает `set -e\r` и падает
# с невнятной ошибкой, потому что \r возвращает курсор в начало строки.
# Фиксируем переводы строк независимо от того, с какой машины сделан коммит.
*.sh text eol=lf
